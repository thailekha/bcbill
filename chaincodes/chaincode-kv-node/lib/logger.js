const cowsay = require('cowsay');

function _l(...stuff) {
  // const text = stuff
  //   .map(s => (typeof s === 'object') ? JSON.stringify(s) : s)
  //   .join('\n');
  // console.log(cowsay.say({text}));
}

module.exports = _l;